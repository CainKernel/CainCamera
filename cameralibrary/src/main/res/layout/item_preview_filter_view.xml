<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_filter_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_gravity="bottom"
    android:layout_marginTop="@dimen/dp5"
    android:gravity="bottom">

    <FrameLayout
        android:id="@+id/item_filter_panel"
        android:layout_width="@dimen/preview_item_size"
        android:layout_height="@dimen/preview_item_size"
        android:layout_gravity="center"
        android:padding="@dimen/dp2">
        <ImageView
            android:id="@+id/item_filter_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:layout_gravity="center" />
    </FrameLayout>

    <TextView
        android:id="@+id/item_filter_name"
        android:layout_width="@dimen/preview_item_size"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dp5"
        android:layout_marginLeft="@dimen/dp10"
        android:layout_marginRight="@dimen/dp10"
        android:layout_gravity="bottom|center_horizontal"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:textAlignment="center" />
</LinearLayout>